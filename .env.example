# ===== OPENAI (LLM) =====
# Copie este arquivo para .env e informe suas credenciais reais.
OPENAI_API_KEY="SUA_CHAVE_DA_API_DA_OPENAI_AQUI"

# ===== BROWSER (Playwright) =====
# Navegador padrão: chromium, firefox ou webkit
DEFAULT_BROWSER="chromium"

# ===== WHATSAPP BUSINESS API (Meta) =====
# Obtenha essas informações em https://business.facebook.com/wa/manage/
# Token de acesso (Bearer token da API)
WHATSAPP_TOKEN="SEU_TOKEN_WHATSAPP_AQUI"

# ID do número de telefone (Phone Number ID)
WHATSAPP_PHONE_ID="SEU_PHONE_NUMBER_ID_AQUI"

# ID da conta de negócios (Business Account ID) - opcional
WHATSAPP_ACCOUNT_ID="SEU_ACCOUNT_ID_AQUI"

# Número de teste para envios (formato: +55 DDD NNNNNNNNN)
WHATSAPP_TEST_NUMBER="+1 555 632 2287"

# ===== TELEGRAM BOT API =====
# Obtenha essas informações em https://t.me/BotFather
# Token do bot (ex: 123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11)
TELEGRAM_BOT_TOKEN="SEU_BOT_TOKEN_AQUI"

# Chat ID para testes (seu ID de usuário ou ID do grupo)
# Dica: converse com @userinfobot para obter seu ID
TELEGRAM_TEST_CHAT_ID="123456789"

# ===== GOOGLE CALENDAR (Service Account) =====
# Caminho para o JSON da Service Account (recomendado) ou outro método de autenticação
GOOGLE_SERVICE_ACCOUNT_FILE="/caminho/para/service-account.json"
# ID do calendário (ex: primary ou um calendarId específico)
GOOGLE_CALENDAR_ID="primary"

# ===== EMAIL / SMTP =====
# Exemplo para SMTP comum (Gmail SMTP exige configuração específica ou App Password)
EMAIL_SMTP_HOST="smtp.example.com"
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USER="seu_usuario_smtp"
EMAIL_SMTP_PASSWORD="sua_senha_smtp"
SENDER_EMAIL="seu@email.com"

# ===== GMAIL API (opcional) =====
# Para envio via Gmail API você pode usar Service Account + domain-wide delegation
# (requer GSuite/Workspace e configuração de delegação) ou um arquivo OAuth.
GMAIL_SERVICE_ACCOUNT_FILE="/caminho/para/gmail-service-account.json"
# Email do usuário delegado (usuário da GSuite que receberá permissão para enviar)
GMAIL_DELEGATED_USER="usuario@seudominio.com"
# Alternativa: arquivo com credenciais OAuth do usuário (authorized user token).
# Para gerar/atualizar este arquivo use:
#   python -m src.integrations.setup_gmail_oauth --credentials-file credentials.json
GMAIL_CREDENTIALS_FILE="/caminho/para/gmail_authorized_user.json"
